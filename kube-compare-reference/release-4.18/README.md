# Overview

- metadata-rds.yaml: a copy from rds/metadata.yaml, used by script to compare against RDS.
- metadata-blueprint.yaml: a copy of metadata-rds.yaml as a base, it can/should be customized to add/update/delete certain resources from the references so to clear the noise, get accurate result or check more partner specific settings. 

